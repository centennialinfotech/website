RewriteEngine On

# Remove  extension
RewriteCond %{THE_REQUEST} \s/([^.]+)\ [NC]
RewriteRule ^ /%1 [R=301,L]

# Internally map clean URL to  file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^(.*)$ $1 [L]